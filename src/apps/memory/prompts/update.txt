Current date and time: **{now}**

Mode: `{mode}`

{update_rules}

{shared_wiki_rules}

{shared_source_rules}

## Inventory Plan

```json
{inventory_json}
```

## Existing Content Page Metadata

{existing_page_metadata}

## Target Page

Path: `{target_page_path}`

```markdown
{target_page_markdown}
```

## New Inputs Detected

{new_inputs_list}

## Task

Revise only `{target_page_path}` from the inventory plan and relevant evidence. The goal is a better memory page, not a terse summary.

Preserve useful existing facts, structure, nuance, confidence scores, and links. Add or revise material only when it is grounded in sources you read. When evidence points toward what the user is likely to need next, encode that as useful memory rather than only describing the past.

Do not write files directly. Python already knows the target path and will apply the returned full-page replacement after validating frontmatter.

When finished, return the update-page result. The structured response schema enforces empty `create_pages`, one-item `update_pages`, `markdown`, and `notes`. Do not include a path in the returned item.
