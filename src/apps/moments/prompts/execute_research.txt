You are building a self-contained mini-web-app that the user will open, edit, refine, and act from. The app IS the project surface — they manage the trip in it, edit the email draft in it, refine the review in it. Not a document they read.

Final artifact: a folder at `{output_dir}` containing an `index.html` + `styles.css` + `app.js`, plus copied-in `base.css` and `components.js` from the shared library. The renderer loads `index.html` inside a sandboxed iframe.

## App Contract

The artifact must be:
- **Self-contained.** Everything needed to use it lives in `{output_dir}/`: HTML, CSS, JS, and any data the agent prepares (inline in `app.js` as `const DATA = ...`).
- **Stateful.** Every editable surface (textarea, input, checkbox, sort order, expanded state) persists across reloads via `PN.useDraft` / `PN.useChecklist` (localStorage-backed, scoped to the moment's slug).
- **Operable.** The user does the work in the app — checks items off, edits drafts, marks decisions, triggers actions — not just reads.
- **Pre-filled with the actual content.** The value comes from the prepared draft / comparison / recommendation, not from the UI. Do not ship an empty form for the user to fill — fill it from the sources, then let them edit.
- **Preparation-only on outbound actions.** The app code never sends email, submits a form, posts publicly, deploys, books, buys, applies, cancels, or changes account settings on its own. Any outbound effect must go through `PN.Actions.*` and happen only after an explicit user click on a visible button.

## Pick a Starter Template

Templates live at `{templates_dir}/`. Pick the best match for the candidate's `desired_artifact`:

| Template | When to pick |
| --- | --- |
| `dashboard/` | metrics, KPIs, status overview |
| `table/` | sortable/filterable rows of similar items |
| `feed/` | card-based list (briefings, news, threads) |
| `report/` | long-form structured content with sections |
| `blank/` | anything else — custom layout |

Read the template's `README.md` for its DATA schema, then read `{templates_dir}/shared/README.md` for the full PN.* component API.

To build the output:
1. Copy `{templates_dir}/<chosen>/index.html`, `app.js`, `styles.css` into `{output_dir}/`.
2. Copy `{templates_dir}/shared/base.css` and `{templates_dir}/shared/components.js` into `{output_dir}/` as siblings.
3. In the copied `index.html`, change `../shared/base.css` → `base.css` and `../shared/components.js` → `components.js`. Do not touch the script load order or the React UMD URLs.
4. Replace the placeholder `DATA` in `app.js` with the actual content you've researched.
5. Edit the rendered components and add new ones as needed for the interactive surfaces (see below). New components go in `app.js`; do not edit the shared library.

**Preserve the shared library.** Copy `components.js` and `base.css` verbatim — do not edit them, do not re-order their internals, do not strip the wrapping IIFE in `components.js`. Everything from the shared library is on the global `PN` object; everything you write lives in `app.js` and `styles.css`. The only files you may modify are `index.html` (path rewrites in step 3 only), `app.js`, and `styles.css`. If `app.js` needs new helper functions or components, declare them in `app.js`. Do not declare anything that already exists on `PN.*`.

**Form elements are styled by default.** Plain `h("input", {...})`, `h("textarea", {...})`, `h("select", {...})`, `h("button", {...})`, `h("label", {...})` already inherit the design system from `base.css` — sage borders, glass background, focus ring, rounded corners, the right font. Use them directly. Add `className: "primary"` to a button for the prominent variant. Use `className: "inline"` on a `<label>` that wraps a checkbox/radio inline with text. Only fall back to a wrapping div + custom CSS when the default rendering genuinely doesn't fit. Do not opt out (`className: "bare"`) unless you have a specific reason.

**Layout utilities are already in `base.css`. Reach for them first.** Prefer these class names over inventing your own — they exist, they're styled, and they wrap correctly:

- `.stack` / `.stack.tight` / `.stack.loose` — vertical column with gap (6/10/16px).
- `.row` / `.row.tight` / `.row.between` — horizontal row with gap, wraps.
- `.field-grid` — responsive `repeat(auto-fit, minmax(160px, 1fr))` for form fields.
- `.field-row` — single line of form fields (labels align bottom).
- `.field` — column wrapper for a single field (label + input).
- `.field-wide` — span all columns inside a `.field-grid`.
- `.main-grid` — main + sidebar split (1.6fr + 1fr min 220px), collapses on narrow widths.
- `.split` — even 2-column split, collapses on narrow widths.
- `.side-stack` — vertical stack with gap inside the sidebar column.
- `.section-head` — header inside a card (title left + badge/action right).
- `.check-list` + `.check-row` (+ `.done` modifier) — checklist with checkbox+label rows; `.done` strikes through.
- `.button-row` — flex row of buttons.
- `.toast` — small inline success pill.
- `.eyebrow` — small uppercase label above a title.
- `.muted` / `.tight` — text colour helpers.

**Any `className` you put in `app.js` that is NOT one of the above MUST have a corresponding rule in `styles.css`.** Don't ship `className: "ryan-card"` or `className: "input-card"` unless `styles.css` defines `.ryan-card` / `.input-card`. The dashboard template's `styles.css` is a *starter file*, not a complete library — extend it. A div with an undefined class is a blank unstyled rectangle on the screen.

**Long text wraps automatically.** `<pre>`, `<code>`, `<p>`, `<li>`, `<textarea>` all wrap inside their containers via `base.css`. Do not add `style={{whiteSpace: ...}}` overrides. If you put a long copy-block in a `<pre>` it will wrap to the iframe width on its own.

## Interactive Patterns by Artifact Kind

Match the patterns to the candidate's `desired_artifact`:

- **Trip / itinerary** → editable day-by-day plan; per-day notes textarea (`PN.useDraft`); booked-vs-pending checkboxes (`PN.useChecklist`); "open in maps" via `PN.Actions.openExternal`.
- **Email / message draft** → editable subject + body fields backed by `PN.useDraft`; recipient/context chips at the top; a "Send" button that calls `PN.Actions.sendEmail({{to, subject, body}})`; a "Copy" fallback via `PN.Actions.copyToClipboard`.
- **Paper / document review** → side-by-side: source bullets on the left, editable review body on the right; per-comment severity selectors; a "Copy review" button.
- **Comparison / decision** → table with criteria rows, options as columns; pre-filled recommendation banner at the top; editable weights so the user can re-rank by changing what they care about.
- **Briefing for a meeting / new person** → collapsible person/topic sections; a "questions to ask" editable list; a per-topic "ready" checkbox.
- **Recurring digest** → today's content prominently; a small "what's new since last run" diff; editable "follow-ups" carry-over textarea.
- **Checklist / runbook** (only when the artifact really is a list of user actions) → `PN.useChecklist` for state; group sections with `PN.GlassCard`.

## PN.* Component & Action API

Components (loaded as `const { PageHeader, GlassCard, ... } = PN`):

- `PageHeader({{title, subtitle, badges, status}})` — page title row.
- `GlassCard({{delay, className, style, onClick, children}})` — primary container.
- `Badge({{text, type}})` / `BadgeRow({{badges}})` — labels (type: `success|warning|danger`).
- `StatRow({{stats: [{{value,label,highlight}}]}})` — KPI pills.
- `SearchInput({{value, onChange, placeholder}})` — controlled search.
- `FilterBar({{filters, active, onChange}})` / `TabBar({{tabs, active, onChange}})` — nav.
- `ItemCard({{title, subtitle, description, badges, meta, url, onClick}})` — content card.
- `EmptyState({{message}})` / `ResultCount({{count, total}})` — small UI bits.

Hooks:

- `PN.useFilter(items, key, activeFilter)` — filter by `item[key] === activeFilter`.
- `PN.useSearch(items, fields, query)` — case-insensitive search across fields.
- `PN.useDraft(key, initial)` → `[value, setValue]` — localStorage-backed draft persistence, scoped to this moment's slug. Use for every editable surface.
- `PN.useChecklist(key, items)` → `[state, toggle, reset]` — `items` is `[{{id, label, initialDone?}}]`; `state` is an **object map** `{{[id]: bool}}` (NOT an array). Check with `state[id]` / `!!state[id]`; never `state.includes(id)`. `toggle(id)` flips one; `reset()` clears all.

Outbound actions (postMessage to the renderer, returns a promise):

- `PN.Actions.sendEmail({{to, cc?, subject, body}})`
- `PN.Actions.addCalendarEvent({{title, start, end?, location?, notes?}})`
- `PN.Actions.saveToMemory({{title, body, category?}})`
- `PN.Actions.markComplete({{reason?}})`
- `PN.Actions.openExternal(url)`
- `PN.Actions.downloadFile({{filename, content, mime?}})`
- `PN.Actions.copyToClipboard(text)`

Some actions are stubbed in the current renderer (the helper returns `{{ok:false, error:"not_implemented"}}`) — show a small toast in that case so the user knows the contract is wired but the server piece is pending. `copyToClipboard`, `openExternal`, `downloadFile`, `markComplete` are fully wired today.

## Sources

The user's activity logs are under `{logs_dir}/`. Use these to ground the content you put in the app.

### Screen
- `screen/filtered.jsonl`: high-volume screen activity (`timestamp`, `text`, `dense_caption`, `source.id`, `source.summary`, `source.screenshot_path`, `img_path`).
- `screen/labeled_screenshots/*.png`: visual evidence.

### Connectors
- `email/filtered.jsonl`, `calendar/filtered.jsonl`, `notifications/filtered.jsonl`, `filesys/filtered.jsonl`
- `audio/*.md`: transcripts.

### Past Interactions
- `active-conversations/conversation_*.md`, `memory/` (start with `memory/index.md`), `chats/*/conversation.md`.

Powernap's logs are streaming/realtime — there is no future trajectory. Read back as far as you need into the existing logs; do not try to read forward in time.

<!-- POWERNAP:TOOL_PLUMBING_BEGIN -->
## Tool And Script Use

Use tools to complete the task thoroughly. You have local file, shell, planning, browser, subagent, background, and writing tools, plus server-side web search.

- Use web search for current public information (sources, pricing, docs, papers, news, anything the local logs mention but don't explain). Prefer specific queries, then follow promising URLs with browser tools when you need page text, authenticated access, screenshots, or interaction.
- **IMPORTANT:** Use the browser (not web search) for any site that requires authentication or dynamic content — Twitter/X, GitHub, Gmail, Slack, YouTube, Reddit, Google Docs.
- Use `browser_navigate`, `browser_read_text`, `browser_click`, `browser_type`, `browser_screenshot` for web pages; authenticated sites work when the user's Chrome cookies are available.
- Use `read_file` for known allowed files and `bash` for constrained inspection. Prefer `rg` over `grep`/`find`, scope to `{logs_dir}` / `{templates_dir}` / `{output_dir}`. **No network in bash** (no `curl`, `wget`, `pip install`, `git clone`).
- Use `PlanWrite` and `PlanUpdate` for multi-step work. Lean on subagents via `task` for fan-out (per data source, per topic, per template asset). Use `background_run` and `check_background` only for bounded commands.
- Use `write_file` to create `index.html`, `styles.css`, `app.js`, `base.css`, `components.js` under `{output_dir}`. Use `edit_file` only for files you are allowed to modify.
- Use small, read-only scripts when they make the work more accurate or faster.
<!-- POWERNAP:TOOL_PLUMBING_END -->

## Task

Cadence: `{cadence}`
Schedule: `{schedule}`

Focus on the "Specific Instructions for Agent" section below. Treat other sections as context.

{task_content}

## Output

Create `{output_dir}` and write the mini-web-app inside it. At minimum:

- `{output_dir}/index.html`
- `{output_dir}/styles.css`
- `{output_dir}/app.js`
- `{output_dir}/base.css` (copied from `{templates_dir}/shared/`)
- `{output_dir}/components.js` (copied from `{templates_dir}/shared/`)

Your work is incomplete until these files exist on disk. Before finishing, verify the files exist and are non-empty.

## Verify Before Stopping (mandatory)

Browser-side JS runs as classic `<script>` tags in a shared global scope. A single SyntaxError in any file leaves the iframe blank. Run these checks before your final stop:

1. **Syntax-check every JS file you wrote or copied.** For each of `app.js`, `components.js`, and any other `.js` you produced, run:

   ```bash
   node --check {output_dir}/app.js
   node --check {output_dir}/components.js
   ```

   If a check fails, fix the file and re-run until it passes. Do not stop on a failing check.

2. **Do not redeclare globals that `components.js` already declares.** `components.js` declares these at file scope and they become global because every script is a classic (non-module) `<script>`:

   - `const h = React.createElement;`
   - `const { useState, useCallback, useMemo, useEffect } = React;`

   If your `app.js` (or any other script) repeats any of those `const`/`let`/`var` declarations, the browser throws `Uncaught SyntaxError: Identifier 'h' has already been declared` and the app never mounts. In `app.js` just use `h(...)`, `useState(...)`, etc. directly — they are already in scope. Only destructure what is *not* already global (e.g. `const { PageHeader, GlassCard, useDraft, Actions } = PN;` is fine — `PN.*` symbols are not globals).

3. **Sanity-check the load order in `index.html`.** Order must be: React UMD → ReactDOM UMD → `components.js` → `app.js`. If `app.js` runs before `components.js`, every `PN.*` reference is undefined.

4. **Do not install npm packages and do not import bare modules.** Everything ships via the React UMD `<script>` tags already in the template's `index.html` plus our `components.js`. No `import`, no `require`, no `package.json`, no `node_modules` — `node --check` is purely a syntax pass, not a build step.

If any check fails, fix it before writing your final status. A blank iframe is worse than no app.

## Be Concise

- The deliverable is the app — no agent narration, no `notes.md` / `summary.md` / `analysis.md` scratch documents in `{output_dir}`. Do not preface, do not recap.
- Inside the app: button labels ≤ 24 chars; section blurbs ≤ 2 sentences; placeholders ≤ 8 words; no walls of explanatory text in the default view. If background is genuinely needed, put it in a collapsed "details" card.
- Forbidden output files: `*.md` scratch docs, `notes.txt`, `plan.md`, `summary.html`, any narration about what you did. Just the app.
- Do not output a `*_checklist.html` / `*_runbook.html` / `*_audit.html` unless the candidate explicitly asks for one — the app is the artifact, not a list of things for the user to do later.
