Current date and time: **{now}**

Mode: `{mode}`
Last discovery checkpoint: **{last_discovery_date}**
Activity window starts after: **{activity_since_date}**

{discover_rules}

{shared_executor_capabilities}

{shared_quality_bar}

{shared_sources}

{shared_moments}

## Existing Accepted Moments

{accepted_moments}

## Feedback And State Signals

{feedback_state_summary}

## Draft State For This Chunk

{draft_context}

## Activity Chunk Metadata

{chunk_metadata}

## Activity Chunk

{activity_chunk}

## Task

You are the creative ideation step for one chronological activity chunk. Use this chunk to predict likely next needs and propose useful moment ideas that extend the user's work beyond what is already visible.

Return idea cards only. A separate compiler step converts your ideas into draft candidates.

Think in terms of future leverage. Each idea should answer:
- What is the user likely moving toward?
- What artifact would make that future step easier, sharper, or less likely to miss important context?
- When exactly would the user open or use that artifact?
- What evidence gives the executor a grounded starting point?
- What concrete benefit would the artifact create for that future use?

Favor ideas that would still be useful tomorrow: research synthesis, preparation, comparison, prioritization, design framing, experiment planning, or recurring context the user would otherwise rebuild by hand.

Prefer artifacts with one main job and a visible shape: sections, comparison axes, questions to answer, recommendations to produce, or decision criteria.

If several outputs serve the same future use, represent them as sections of one named artifact.

Return a compact set of the strongest ideas from this chunk, usually 0-2. Quality matters more than coverage.

Before adding a second idea, check that it serves a different future use, depends on a different evidence base, and produces a different artifact. If one broader artifact would serve both, return the broader idea.

Use this evidence threshold:
- One dense work signal can support a moment when it already contains enough context for a useful artifact.
- Sparse signals become moments when multiple signals converge on the same future need.
- Single sparse leads are useful notes; mark them `weak` until more context appears.

Write evidence as short factual claims with pointers, for example `screen/filtered.jsonl:123 at 2026-05-01 10:15 shows the user comparing two papers`. Use `source_paths` for pointer-only references.

For `relation_to_existing`, use:
- `new` for a distinct new idea.
- `possible_update` when the idea may refresh an accepted moment or current draft.
- `duplicate` when it looks covered already.
- `weak` when it is still a low-confidence lead.

End with a single fenced JSON block. It must be the last thing in your final message.

```json
{{
  "ideas": [
    {{
      "likely_next_need": "What the user will probably need next, stated as a concrete future-facing need",
      "title": "Short useful idea title",
      "topic_hint": "kebab-or-plain-topic",
      "artifact": "Concrete bounded artifact the executor should produce, with one main job",
      "why_useful": "Why this would save time, reduce missed information, clarify a decision, or make recurring work easier",
      "evidence": ["source/path:line at timestamp shows the user doing or needing X"],
      "source_paths": ["relative/source/path:line"],
      "cadence_hint": "once",
      "relation_to_existing": "new"
    }}
  ],
  "notes": "Short note about searches, files, or reasoning that informed the ideas."
}}
```
